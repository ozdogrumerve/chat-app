# TChat – Requirements
# Requires Python 3.8+
# All dependencies are part of the Python standard library — no installation needed.

# socket    – network communication
# threading – concurrent operations
# tkinter   – GUI (bundled with Python)
# re        – regex (message parsing)
# datetime  – timestamps